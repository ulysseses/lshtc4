parents_index
	{ child -> set of parents }
	{ int   ->     set(int)   }

children_index
	{ parent -> set of children  }
	{ int    ->  set of parents  }

X
	list (corpus) of tf-idf'd bag-of-word dicts (docs)
	[ { hash_id : tf    } ]
	[ {    int  : float } ]

Y
	[ [label0, label1, ...]...]
	[ [int, int, ... ] ... ...]

